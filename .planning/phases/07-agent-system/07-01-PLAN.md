---
phase: 07-agent-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - 'Users can define agents via markdown templates with YAML frontmatter'
    - 'Agent templates are validated for required fields and tool references'
    - 'Templates are stored and retrievable by name'
  artifacts:
    - path: 'packages/@baniya/agents/src/types.ts'
      provides: 'AgentTemplate interface, ToolDefinition interface'
    - path: 'packages/@baniya/agents/src/validator.ts'
      provides: 'Template validation with Zod'
    - path: 'packages/@baniya/agents/src/storage.ts'
      provides: 'Template CRUD operations'
  key_links:
    - from: 'packages/@baniya/agents/src'
      to: 'packages/@baniya/types'
      via: 'import from @baniya/types'
---

<objective>
Create the agent template system: template format, validation schema, and storage layer.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-system/07-CONTEXT.md
@.planning/phases/07-agent-system/07-RESEARCH.md

# Reference: Python agent example

@PythonAIAgentFromScratch/main.py
</context>

<tasks>

<task type="auto">
  <name>Create @baniya/agents package</name>
  <files>packages/@baniya/agents/package.json, packages/@baniya/agents/tsconfig.json</files>
  <action>
Create new package `packages/@baniya/agents` with:
- package.json: name "@baniya/agents", dependencies on @baniya/types, zod, gray-matter (for frontmatter parsing)
- tsconfig.json: extends tsconfig.base.json
</action>
  <verify>ls packages/@baniya/agents/</verify>
  <done>Package structure created with package.json and tsconfig.json</done>
</task>

<task type="auto">
  <name>Define agent types</name>
  <files>packages/@baniya/agents/src/types.ts</files>
  <action>
Create TypeScript interfaces:
- ToolDefinition: { name, description, inputSchema, outputSchema, execute(params) }
- AgentTemplate: { name, description, version, tools[], model, maxIterations, instructions, outputSchema }
- AgentExecution: { id, templateId, input, context, state, history }
</action>
  <verify>cat packages/@baniya/agents/src/types.ts | head -50</verify>
  <done>Types exported: AgentTemplate, ToolDefinition, AgentExecution</done>
</task>

<task type="auto">
  <name>Create template validator</name>
  <files>packages/@baniya/agents/src/validator.ts</files>
  <action>
Create Zod-based validator:
- validateTemplate(template): returns validated template or throws
- Checks: name required, tools array valid, model in [auto, local, hybrid, cloud]
- Validates instructions is non-empty string
- Validates maxIterations is 1-100
</action>
  <verify>pnpm --filter @baniya/agents build</verify>
  <done>Validator exported and package builds successfully</done>
</task>

<task type="auto">
  <name>Implement template storage</name>
  <files>packages/@baniya/agents/src/storage.ts</files>
  <action>
Create template storage class:
- save(template: AgentTemplate): Promise<void>
- get(name: string): Promise<AgentTemplate>
- list(): Promise<AgentTemplate[]>
- delete(name: string): Promise<void>
- Storage: In-memory Map with optional file-based persistence
</action>
  <verify>import { AgentStorage } from '@baniya/agents'</verify>
  <done>AgentStorage class exported with save/get/list/delete methods</done>
</task>

<task type="auto">
  <name>Create template parser</name>
  <files>packages/@baniya/agents/src/parser.ts</files>
  <action>
Create parser for markdown templates with YAML frontmatter:
- parseTemplate(content: string): AgentTemplate
- extractFrontmatter(content): { metadata, body }
- serializeTemplate(template): string (round-trip)
</action>
  <verify>node -e "const { parseTemplate } = require('@baniya/agents'); console.log(parseTemplate('---\\nname: test\\n---\\nInstructions'))"</verify>
  <done>Parser handles frontmatter extraction and template serialization</done>
</task>

</tasks>

<verification>
- [ ] @baniya/agents package exists and builds
- [ ] AgentTemplate type matches spec from RESEARCH.md
- [ ] Validator rejects invalid templates with clear errors
- [ ] Storage persists templates across restarts
</verification>

<success_criteria>
Agent template system with:

- YAML frontmatter + Markdown body format
- Zod validation for required fields
- CRUD storage for templates
- Build passes: pnpm -r build
  </success_criteria>

<output>
After completion, create `.planning/phases/07-agent-system/07-01-SUMMARY.md`
</output>
