---
phase: 07-agent-system
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - 'Pre-built agent templates are available for common tasks'
    - 'Users can import and customize templates'
    - 'Templates follow GSD-style patterns (role, instructions, output)'
  artifacts:
    - path: 'packages/@baniya/agents/templates/researcher.md'
      provides: 'Researcher agent template'
    - path: 'packages/@baniya/agents/templates/planner.md'
      provides: 'Planner agent template'
    - path: 'packages/@baniya/agents/templates/reviewer.md'
      provides: 'Code reviewer agent template'
    - path: 'packages/@baniya/agents/src/templates/builtin.ts'
      provides: 'Built-in template loader'
  key_links:
    - from: 'packages/@baniya/agents/templates'
      to: 'packages/@baniya/agents/src/storage'
      via: 'TemplateStorage.importTemplate()'
---

<objective>
Create pre-built agent templates and template library loader.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-agent-system/07-CONTEXT.md
@.planning/phases/07-agent-system/07-RESEARCH.md
@.planning/phases/07-agent-system/07-01-PLAN.md
@.planning/phases/07-agent-system/07-02-PLAN.md

# Reference: GSD agent patterns from context

</context>

<tasks>

<task type="auto">
  <name>Create Researcher agent template</name>
  <files>packages/@baniya/agents/templates/researcher.md</files>
  <action>
Create Researcher agent template:
```yaml
---
name: researcher
description: Research agent that gathers information on topics
version: 1.0.0
tools: [search_web, read_file, write_file]
model: auto
max_iterations: 10
---

## Role

You are a research assistant that helps gather and synthesize information from multiple sources.

## Instructions

1. First understand the user's research goal - ask clarifying questions if needed
2. Use search to find relevant information sources
3. Read key sources for detailed information
4. Organize findings by theme
5. Synthesize into a clear summary with citations

## Output Format

Return structured JSON:
{
"topic": "...",
"summary": "...",
"sources": [{"url": "...", "title": "...", "relevance": "..."}],
"findings": [{"theme": "...", "details": "...", "confidence": "high|medium|low"}]
}

````

Use search_web tool to find information, read_file to examine documents.
</action>
  <verify>cat packages/@baniya/agents/templates/researcher.md | head -20</verify>
  <done>Researcher template created with frontmatter and instructions</done>
</task>

<task type="auto">
  <name>Create Planner agent template</name>
  <files>packages/@baniya/agents/templates/planner.md</files>
  <action>
Create Planner agent template:
```yaml
---
name: planner
description: Breaks tasks into phases with actionable plans
version: 1.0.0
tools: [read_file, list_directory, execute_command]
model: auto
max_iterations: 5
---

## Role
You are a planning specialist that breaks complex tasks into executable phases.

## Instructions
1. Analyze the task and understand the goal
2. Identify dependencies and constraints
3. Break into sequential phases
4. Each phase should have 2-3 tasks
5. Consider parallel execution where possible

## Output Format
Return structured JSON:
{
  "task": "...",
  "phases": [
    {
      "name": "phase-1",
      "objective": "...",
      "tasks": [{"name": "...", "action": "...", "depends_on": []}],
      "wave": 1
    }
  ],
  "dependencies": [...],
  "estimated_complexity": "low|medium|high"
}
````

</action>
  <verify>cat packages/@baniya/agents/templates/planner.md | head -20</verify>
  <done>Planner template created with phase breakdown format</done>
</task>

<task type="auto">
  <name>Create Code Reviewer agent template</name>
  <files>packages/@baniya/agents/templates/reviewer.md</files>
  <action>
Create Code Reviewer agent template:
```yaml
---
name: reviewer
description: Reviews code for issues, bugs, and improvements
version: 1.0.0
tools: [read_file, glob, execute_command]
model: auto
max_iterations: 8
---

## Role

You are a code review specialist focused on quality, security, and best practices.

## Instructions

1. Examine the codebase structure first
2. Review code for:
   - Security vulnerabilities (SQL injection, XSS, hardcoded secrets)
   - Error handling gaps
   - Performance issues
   - Code quality (naming, duplication, complexity)
   - Best practices violations
3. Use glob to find relevant files
4. Read key files to understand context

## Output Format

Return structured JSON:
{
"files_reviewed": [...],
"issues": [
{"severity": "critical|high|medium|low", "file": "...", "line": "...", "type": "...", "description": "...", "recommendation": "..."}
],
"summary": "...",
"score": 1-10
}

```
</action>
  <verify>cat packages/@baniya/agents/templates/reviewer.md | head -20</verify>
  <done>Reviewer template created with security and quality checks</done>
</task>

<task type="auto">
  <name>Create template library loader</name>
  <files>packages/@baniya/agents/src/templates/builtin.ts</files>
  <action>
Create built-in template loader:
- loadTemplate(name: string): Promise<AgentTemplate>
- listBuiltin(): { name, description }[]
- importAll(): Promise<void> (imports all templates to storage)
- Templates stored as static strings in the module

Uses parseTemplate() from parser.ts to convert markdown to AgentTemplate.
</action>
  <verify>cat packages/@baniya/agents/src/templates/builtin.ts | head -20</verify>
  <done>Template loader with listBuiltin and loadTemplate functions</done>
</task>

<task type="auto">
  <name>Verify all templates validate</name>
  <files>packages/@baniya/agents/src/validator.ts</files>
  <action>
Run validation on all built-in templates:
- Load each template with parseTemplate()
- Validate with validateTemplate()
- Fix any schema issues

Ensure: name, tools reference valid tool names, instructions non-empty.
</action>
  <verify>pnpm --filter @baniya/agents build</verify>
  <done>All templates validate successfully</done>
</task>

</tasks>

<verification>
- [ ] Researcher template exists and validates
- [ ] Planner template exists and validates
- [ ] Reviewer template exists and validates
- [ ] Templates loadable via builtin.ts
</verification>

<success_criteria>
Template library:
- 3 pre-built templates (Researcher, Planner, Reviewer)
- GSD-style format (Role, Instructions, Output)
- Loadable via AgentStorage
- All validate successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-system/07-04-SUMMARY.md`
</output>
```
