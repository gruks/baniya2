---
phase: 07-agent-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - 'Agents can use registered tools to operate on the filesystem'
    - 'Tool execution is sandboxed to prevent security issues'
    - 'Tool results are returned in structured format'
  artifacts:
    - path: 'packages/@baniya/agents/src/registry.ts'
      provides: 'Tool registration and discovery'
    - path: 'packages/@baniya/agents/src/tools/file-tools.ts'
      provides: 'read_file, write_file, list_directory tools'
    - path: 'packages/@baniya/agents/src/tools/command-tools.ts'
      provides: 'execute_command with whitelist validation'
    - path: 'packages/@baniya/agents/src/sandbox.ts'
      provides: 'Security sandbox for tool execution'
  key_links:
    - from: 'packages/@baniya/agents/src/tools'
      to: 'packages/@baniya/agents/src/registry'
      via: 'registerTool()'
    - from: 'packages/@baniya/agents/src'
      to: 'packages/@baniya/llm-router'
      via: 'BaniyaRouter for agent AI calls'
---

<objective>
Build the tool registry with core built-in tools for file operations and command execution.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-agent-system/07-CONTEXT.md
@.planning/phases/07-agent-system/07-RESEARCH.md
@.planning/phases/07-agent-system/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create tool registry</name>
  <files>packages/@baniya/agents/src/registry.ts</files>
  <action>
Create ToolRegistry class:
- register(tool: ToolDefinition): void
- get(name: string): ToolDefinition
- list(): ToolDefinition[]
- has(name: string): boolean
- Tools stored in Map<string, ToolDefinition>
</action>
  <verify>cat packages/@baniya/agents/src/registry.ts | grep "class ToolRegistry"</verify>
  <done>ToolRegistry class with register/get/list/has methods</done>
</task>

<task type="auto">
  <name>Implement file tools</name>
  <files>packages/@baniya/agents/src/tools/file-tools.ts</files>
  <action>
Create file operation tools:
- read_file(path: string): { content: string, size: number }
- write_file(path: string, content: string): { success: boolean, path: string }
- list_directory(path: string): { files: string[], dirs: string[] }
- glob(pattern: string): { matches: string[] }

Security: Validate paths, max file size 1MB, no path traversal (..)
</action>
<verify>pnpm --filter @baniya/agents build</verify>
<done>File tools implemented with security validation</done>
</task>

<task type="auto">
  <name>Implement command tool</name>
  <files>packages/@baniya/agents/src/tools/command-tools.ts</files>
  <action>
Create execute_command tool:
- execute_command(command: string, args?: string[]): { stdout: string, stderr: string, exitCode: number }

Security:

- Whitelist allowed commands: git, node, pnpm, npm, python, cat, ls, echo, mkdir, grep
- Block dangerous: rm -rf, curl | sh, wget | sh, sudo, ssh
- Timeout: 30 seconds max
- Working directory: project root only
  </action>
  <verify>node -e "const { executeCommand } = require('@baniya/agents'); executeCommand('echo hello')"</verify>
  <done>Command execution with security whitelist</done>
  </task>

<task type="auto">
  <name>Create tool sandbox</name>
  <files>packages/@baniya/agents/src/sandbox.ts</files>
  <action>
Create Sandbox class for secure execution:
- execute(toolName: string, params: object): Promise<ToolResult>
- validatePath(path: string): boolean (no traversal)
- validateCommand(cmd: string): boolean (whitelist check)
- applyTimeout(ms: number): Promise<ToolResult>
</action>
  <verify>cat packages/@baniya/agents/src/sandbox.ts | grep "class Sandbox"</verify>
  <done>Sandbox class with path validation and command whitelist</done>
</task>

<task type="auto">
  <name>Register default tools</name>
  <files>packages/@baniya/agents/src/index.ts</files>
  <action>
Update package index to:
- Export ToolRegistry, all tool functions
- Create default registry instance with built-in tools registered
- Export getDefaultTools() function
</action>
  <verify>cat packages/@baniya/agents/src/index.ts</verify>
  <done>Default tools exported and available via import</done>
</task>

</tasks>

<verification>
- [ ] All tools registered and retrievable by name
- [ ] Path validation prevents directory traversal
- [ ] Command whitelist blocks dangerous commands
- [ ] Tool execution has timeout limits
</verification>

<success_criteria>
Tool registry with:

- read_file, write_file, list_directory, execute_command tools
- Security sandbox with path/command validation
- Tools accessible via ToolRegistry.get()
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/07-agent-system/07-02-SUMMARY.md`
</output>
