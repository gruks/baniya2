---
phase: 07-agent-system
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: true

must_haves:
  truths:
    - 'Workflows can include agent nodes for execution'
    - 'agent.execute node runs agent with input and returns structured result'
    - 'Agent state persists across workflow execution steps'
  artifacts:
    - path: 'packages/@baniya/agents/src/executor.ts'
      provides: 'AgentExecutor class with run() method'
    - path: 'packages/@baniya/nodes/src/registry.ts'
      provides: 'agent.execute and agent.chat node types added'
    - path: 'packages/@baniya/agents/src/handlers/agent-handlers.ts'
      provides: 'Node handler implementations'
  key_links:
    - from: 'packages/@baniya/agents/src'
      to: 'packages/@baniya/workflow-engine'
      via: 'NodeHandler interface'
    - from: 'packages/@baniya/agents/src'
      to: 'packages/@baniya/llm-router'
      via: 'BaniyaRouter for LLM calls'
---

<objective>
Add agent node types to Baniya's node registry and implement execution handlers.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-agent-system/07-CONTEXT.md
@.planning/phases/07-agent-system/07-RESEARCH.md
@.planning/phases/07-agent-system/07-01-PLAN.md
@.planning/phases/07-agent-system/07-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create agent executor</name>
  <files>packages/@baniya/agents/src/executor.ts</files>
  <action>
Create AgentExecutor class:
- constructor(tools: ToolRegistry, router: BaniyaRouter)
- async run(template: AgentTemplate, input: object, context: object): Promise<AgentResult>
- Implements ReAct loop: Think → Action → Observe → Final
- Tracks state: { thought, action, observation, history }
- Respects maxIterations limit

Returns: { result: object, artifacts: object[], logs: string[] }
</action>
<verify>cat packages/@baniya/agents/src/executor.ts | grep "class AgentExecutor"</verify>
<done>AgentExecutor with ReAct loop implementation</done>
</task>

<task type="auto">
  <name>Add agent node types to registry</name>
  <files>packages/@baniya/nodes/src/registry.ts</files>
  <action>
Add two new node types:
- agent.execute: { agentId, input, context } → { result, artifacts, logs }
- agent.chat: { agentId, message, history } → { response, state }

Node metadata in registry:

```typescript
{
  type: 'agent.execute',
  category: 'agent',
  label: 'Execute Agent',
  description: 'Run an agent with structured input',
  color: '#8B5CF6',  // purple for agents
  icon: '🤖',
  handles: { inputs: ['input'], outputs: ['result', 'error'] },
  configSchema: {
    agentId: { type: 'text', required: true },
    maxIterations: { type: 'number', default: 10 }
  }
}
```

</action>
  <verify>grep -n "agent.execute" packages/@baniya/nodes/src/registry.ts</verify>
  <done>Agent node types registered in @baniya/nodes</done>
</task>

<task type="auto">
  <name>Implement node handlers</name>
  <files>packages/@baniya/agents/src/handlers/agent-handlers.ts</files>
  <action>
Create node handlers matching NodeHandler interface:
- executeAgentNode(input, config, context): Promise<{ main, error }>
- chatAgentNode(input, config, context): Promise<{ main, error }>

Handlers:

1. Load agent template from storage
2. Create AgentExecutor with tools
3. Run agent with input/context
4. Return result as output
   </action>
   <verify>pnpm --filter @baniya/agents build</verify>
   <done>Node handlers implemented and package builds</done>
   </task>

<task type="auto">
  <name>Register handlers in workflow engine</name>
  <files>packages/@baniya/workflow-engine/src/handlers/index.ts</files>
  <action>
Register agent handlers in workflow engine:
- Import handlers from @baniya/agents
- Add to handler map: { 'agent.execute': executeAgentNode, 'agent.chat': chatAgentNode }
- Ensure handlers are loaded on engine startup
</action>
  <verify>grep "agent" packages/@baniya/workflow-engine/src/handlers/index.ts</verify>
  <done>Agent handlers registered in workflow engine</done>
</task>

</tasks>

<verification>
- [ ] AgentExecutor implements ReAct loop
- [ ] Node types exist in registry
- [ ] Workflow can include agent nodes
- [ ] Agent execution returns structured results
</verification>

<success_criteria>
Agent node types:

- agent.execute runs agent with input, returns result
- agent.chat provides conversational interface
- Handlers integrate with workflow engine
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/07-agent-system/07-03-SUMMARY.md`
</output>
