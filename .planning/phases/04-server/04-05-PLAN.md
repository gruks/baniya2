---
phase: 04-server
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/routes/webhooks.ts
  - apps/server/src/routes/filesystem.ts
  - apps/server/src/validation/schemas.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'webhooks.ts routes have Zod validation'
    - 'filesystem.ts routes have Zod validation'
  artifacts:
    - path: 'apps/server/src/routes/webhooks.ts'
      provides: 'Webhook trigger routes with validation'
      min_lines: 85
    - path: 'apps/server/src/routes/filesystem.ts'
      provides: 'Filesystem operations with validation'
      min_lines: 307
    - path: 'apps/server/src/validation/schemas.ts'
      provides: 'Webhook and filesystem validation schemas'
      contains: 'webhookTriggerSchema'
      contains_filesystem: 'filesystemWriteSchema'
  key_links:
    - from: 'apps/server/src/routes/webhooks.ts'
      to: 'validate middleware'
      via: 'validate(webhookTriggerSchema)'
    - from: 'apps/server/src/routes/filesystem.ts'
      to: 'validate middleware'
      via: 'validate(filesystemWriteSchema)'
---

<objective>
Add Zod validation to webhooks.ts and filesystem.ts routes that were missed in the initial validation implementation.

Purpose: Complete REQ-14 - "Zod validation on ALL API routes" by closing the gaps identified in verification.
Output: Both webhooks.ts and filesystem.ts routes will have proper input validation.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-server/04-04-VERIFICATION.md
@apps/server/src/routes/webhooks.ts
@apps/server/src/routes/filesystem.ts
@apps/server/src/validation/schemas.ts
@apps/server/src/middleware/validate.ts
</context>

<tasks>

<task type="auto">
  <name>Add webhook and filesystem validation schemas</name>
  <files>apps/server/src/validation/schemas.ts</files>
  <action>
    Add the following Zod schemas to apps/server/src/validation/schemas.ts:

    // Webhook schemas
    export const webhookTriggerSchema = z.object({
      workflowId: z.string().uuid(),
      nodeId: z.string().min(1),
    });

    // Filesystem schemas
    export const filesystemWriteSchema = z.object({
      root: z.string().optional(),
      path: z.string().min(1),
      content: z.string().optional(),
      createDirs: z.boolean().optional(),
    });

    export const filesystemPatchSchema = z.object({
      root: z.string().optional(),
      path: z.string().min(1),
      oldStr: z.string().min(1),
      newStr: z.string(),
    });

    export const filesystemAppendSchema = z.object({
      root: z.string().optional(),
      path: z.string().min(1),
      content: z.string().optional(),
    });

    export const filesystemRenameSchema = z.object({
      root: z.string().optional(),
      from: z.string().min(1),
      to: z.string().min(1),
    });

    export const filesystemMkdirSchema = z.object({
      root: z.string().optional(),
      path: z.string().min(1),
    });

    export const filesystemExecSchema = z.object({
      root: z.string().optional(),
      command: z.string().min(1),
      timeout: z.number().optional(),
    });

    // Query schemas for filesystem GET routes
    export const filesystemQuerySchema = z.object({
      root: z.string().optional(),
      path: z.string().optional(),
      q: z.string().optional(),
      limit: z.coerce.number().optional(),
    });

    export const filesystemDeleteSchema = z.object({
      root: z.string().optional(),
      path: z.string().min(1),
    });

  </action>
  <verify>grep -E "(webhookTriggerSchema|filesystemWriteSchema)" apps/server/src/validation/schemas.ts</verify>
  <done>Validation schemas for webhooks and filesystem exist and can be imported</done>
</task>

<task type="auto">
  <name>Apply validation to webhooks.ts routes</name>
  <files>apps/server/src/routes/webhooks.ts</files>
  <action>
    Import the validate middleware and webhookTriggerSchema:
    import { validate } from '../middleware/validate';
    import { webhookTriggerSchema } from '../validation/schemas';

    Apply validation to both webhook routes:
    - POST /:workflowId/:nodeId - Add validate(webhookTriggerSchema) middleware
    - GET /:workflowId/:nodeId - Add validate(webhookTriggerSchema, 'query') middleware

    Note: For GET route, the workflowId and nodeId come from params (req.params), not query.
    The validation should validate the params. Check if validate middleware supports params or use a different approach.
    If middleware doesn't support params, add manual validation at start of handler:
    webhookTriggerSchema.parse({ workflowId: req.params.workflowId, nodeId: req.params.nodeId });

  </action>
  <verify>grep -E "validate.*webhookTriggerSchema" apps/server/src/routes/webhooks.ts</verify>
  <done>webhooks.ts has validation applied to POST and GET routes</done>
</task>

<task type="auto">
  <name>Apply validation to filesystem.ts routes</name>
  <files>apps/server/src/routes/filesystem.ts</files>
  <action>
    Import the validate middleware and filesystem schemas:
    import { validate } from '../middleware/validate';
    import { 
      filesystemWriteSchema, 
      filesystemPatchSchema, 
      filesystemAppendSchema,
      filesystemRenameSchema, 
      filesystemMkdirSchema, 
      filesystemExecSchema,
      filesystemQuerySchema,
      filesystemDeleteSchema
    } from '../validation/schemas';

    Apply validation to each route:
    - POST /write - validate(filesystemWriteSchema)
    - POST /patch - validate(filesystemPatchSchema)
    - POST /append - validate(filesystemAppendSchema)
    - POST /rename - validate(filesystemRenameSchema)
    - POST /mkdir - validate(filesystemMkdirSchema)
    - POST /exec - validate(filesystemExecSchema)
    - GET /list - validate(filesystemQuerySchema, 'query')
    - GET /read - validate(filesystemQuerySchema, 'query')
    - GET /search - validate(filesystemQuerySchema, 'query')
    - GET /stat - validate(filesystemQuerySchema, 'query')
    - DELETE /delete - validate(filesystemDeleteSchema, 'query')

    Note: For routes using req.query parameters, ensure the schema includes optional root and path fields.

  </action>
  <verify>grep -E "validate\(filesystem" apps/server/src/routes/filesystem.ts | wc -l</verify>
  <done>filesystem.ts has validation applied to all 11 routes</done>
</task>

</tasks>

<verification>
Verify all routes have validation by checking that validate() calls exist in both files:
- apps/server/src/routes/webhooks.ts: should have 2 validate calls
- apps/server/src/routes/filesystem.ts: should have 11 validate calls
</verification>

<success_criteria>
All 13 routes (2 webhooks + 11 filesystem) have Zod validation applied. POST /webhooks/:workflowId/:nodeId, GET /webhooks/:workflowId/:nodeId, and all filesystem routes reject invalid input with 400 status and validation error details.
</success_criteria>

<output>
After completion, create `.planning/phases/04-server/04-05-SUMMARY.md`
</output>
