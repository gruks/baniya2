---
phase: 06-dashboard-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/seed.ts
  - apps/server/src/index.ts
  - apps/editor/src/main.ts
  - apps/editor/src/App.vue
autonomous: true

must_haves:
  truths:
    - 'Demo workflow seeds automatically on first server startup'
    - 'Workflow executes end-to-end from manual trigger to response'
    - 'Aadhaar payload routes to local, never cloud'
    - 'Clean business payload routes to cloud'
    - 'Mixed payload takes hybrid path with PII scrubbed'
    - 'Audit log shows one row per node execution with correct cost and routing'
    - 'Dashboard shows accurate savings vs all-cloud calculation'
    - 'Dark mode persists across page refresh'
    - 'Provider status updates live every 10 seconds'
    - 'WebSocket reconnects automatically on disconnect'
  artifacts:
    - path: 'apps/server/src/seed.ts'
      provides: 'Demo workflow seed function'
      contains: 'seedDemoWorkflow'
    - path: 'apps/server/src/index.ts'
      provides: 'Server startup with seed call'
      contains: 'seedDemoWorkflow'
    - path: 'apps/editor/src/main.ts'
      provides: 'Dark mode initialization on startup'
      contains: 'baniya-dark'
    - path: 'apps/editor/src/api/client.ts'
      provides: 'Axios interceptor with WebSocket reconnect logic'
      exports: ['client']
  key_links:
    - from: 'apps/server/src/index.ts'
      to: 'apps/server/src/seed.ts'
      via: 'seedDemoWorkflow call on startup'
      pattern: 'seedDemoWorkflow'
    - from: 'apps/editor/src/main.ts'
      to: 'localStorage'
      via: 'dark mode class toggle'
      pattern: 'baniya-dark'
    - from: 'apps/editor/src/api/client.ts'
      to: 'window.location'
      via: '401 redirect to login'
      pattern: '401.*login'
---

<objective>
Execute demo workflow seed and verify end-to-end pipeline. Wire dark mode initialization. Verify all routing paths (local, hybrid, cloud) work correctly with audit logging.

Purpose: Complete Phase 6 by proving the core Baniya value proposition works — automatic data sensitivity classification and routing with full audit trail.
Output: Seeded demo workflow, verified routing paths, working dark mode initialization, live provider status
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-polish/06-02-PLAN.md
@.planning/phases/06-dashboard-polish/06-03-PLAN.md
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire demo workflow seed to server startup</name>
  <files>apps/server/src/index.ts</files>
  <action>
    Import seedDemoWorkflow in server startup (apps/server/src/index.ts). After database connection is established and before server starts listening, call seedDemoWorkflow with the first user's ID (or create a default user if none exists). The seed function already exists in apps/server/src/seed.ts and creates the demo workflow only if no workflows exist.

    Ensure the seed runs after TypeORM connection but before app.listen().

  </action>
  <verify>
    Start server with empty database → verify "Demo workflow seeded successfully" appears in console
    Start server again → verify no duplicate seed (workflow count > 0 check works)
  </verify>
  <done>Demo workflow auto-seeds on first startup, no duplicates on subsequent starts</done>
</task>

<task type="auto">
  <name>Task 2: Initialize dark mode on app startup</name>
  <files>apps/editor/src/main.ts</files>
  <action>
    Add dark mode initialization to apps/editor/src/main.ts before app.mount('#app'). Read localStorage.getItem('baniya-dark') and if 'true', add 'dark' class to document.documentElement. This ensures dark mode is applied immediately on page load without flash.

    The Settings.vue already handles toggle and persistence — this is just the startup initialization.

  </action>
  <verify>
    Set localStorage baniya-dark=true → refresh page → dark class present on <html>
    Set localStorage baniya-dark=false → refresh page → dark class absent on <html>
  </verify>
  <done>Dark mode applies immediately on page load based on localStorage, no flash of wrong theme</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end demo workflow execution</name>
  <files></files>
  <action>
    This is a verification task. With server running and database seeded:

    1. Register a user via POST /api/auth/register
    2. Login via POST /api/auth/login to get JWT token
    3. Get workflow list via GET /api/workflows → find demo workflow ID
    4. Execute workflow via POST /api/workflows/:id/execute with payload: { "data": "test" }
    5. Verify execution completes: GET /api/executions/:id shows status 'success'
    6. Verify audit log: GET /api/baniya/audit shows entries for each node
    7. Verify cost summary: GET /api/baniya/cost-summary returns valid numbers

    Test routing paths:
    - Critical data (Aadhaar number) → should route local
    - Public data (business text) → should route cloud
    - Mixed data (business text + email) → should route hybrid with sanitizer

    Note: If local LLMs (Ollama/LM Studio) are not running, local path will fail — this is expected. Document which paths succeeded.

  </action>
  <verify>
    curl -X POST http://localhost:3000/api/workflows/:id/execute -H "Authorization: Bearer <token>" -H "Content-Type: application/json" -d '{"data":"test payload"}'
    Response shows execution started
    GET /api/executions/:id returns completed execution with nodeResults
    GET /api/baniya/audit returns audit entries
  </verify>
  <done>Demo workflow executes end-to-end, audit log populated, cost summary accurate</done>
</task>

<task type="auto">
  <name>Task 4: Verify WebSocket reconnection and provider status polling</name>
  <files></files>
  <action>
    Verify WebSocket and provider status features work:

    1. Start server and editor
    2. Open browser dev tools → Network tab → WS filter
    3. Navigate to dashboard → verify WebSocket connection established
    4. Verify provider status polls every 10 seconds (check Network tab for repeated /api/baniya/providers/status calls)
    5. Stop server → verify WebSocket reconnect attempts (check console for reconnect messages)
    6. Restart server → verify WebSocket reconnects automatically
    7. Execute workflow → verify real-time node status updates appear in UI

    Document which features work and any issues found.

  </action>
  <verify>
    WebSocket connects on dashboard load
    Provider status updates every 10 seconds
    WebSocket reconnects after server restart
    Execution status updates in real-time during workflow run
  </verify>
  <done>WebSocket auto-reconnects, provider status polls every 10s, real-time execution updates work</done>
</task>

</tasks>

<verification>
Phase 6 complete when:
- Demo workflow seeds automatically on first server startup
- Workflow executes end-to-end from trigger to response
- Audit log shows one row per node execution
- Dashboard displays accurate cost savings
- Dark mode persists across page refresh
- Provider status updates live every 10 seconds
- WebSocket reconnects automatically on disconnect
- All API routes reject unauthenticated requests with 401
</verification>

<success_criteria>

- Server starts and seeds demo workflow without errors
- Demo workflow executes successfully (all nodes complete)
- Audit log entries created for each node execution
- Cost summary API returns valid data with savings calculation
- Dark mode applies on page load from localStorage
- WebSocket connection established and reconnects on disconnect
- Provider status polls every 10 seconds
- All Phase 6 requirements (REQ-19 through REQ-29) verified
  </success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-polish/06-04-SUMMARY.md`
</output>
