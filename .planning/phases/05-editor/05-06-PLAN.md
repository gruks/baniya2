---
phase: 05-editor
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/editor/src/components/shared/Badge.vue
  - apps/editor/src/components/shared/Modal.vue
  - apps/editor/src/components/shared/Spinner.vue
  - apps/editor/src/components/shared/EmptyState.vue
  - apps/editor/src/components/canvas/EdgeLabel.vue
  - apps/editor/src/components/canvas/MiniExecutionBadge.vue
  - apps/editor/src/views/WorkflowList.vue
  - apps/editor/src/views/BaniyaDashboard.vue
  - apps/editor/src/components/dashboard/AuditTable.vue
  - apps/editor/src/views/WorkflowEditor.vue
  - apps/editor/src/components/canvas/BaniyaNode.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'Badge.vue is imported and used instead of inline badge classes'
    - 'Modal.vue replaces inline modal markup in WorkflowList.vue'
    - 'Spinner.vue replaces inline spinner markup in loading states'
    - 'EmptyState.vue replaces inline empty state in WorkflowList.vue'
    - 'EdgeLabel.vue is integrated into Vue Flow edge rendering'
    - 'MiniExecutionBadge.vue is integrated into BaniyaNode or execution display'
  artifacts:
    - path: 'apps/editor/src/views/WorkflowList.vue'
      provides: 'Uses Modal.vue, EmptyState.vue, Spinner.vue'
      contains: 'import.*Modal|import.*EmptyState|import.*Spinner'
    - path: 'apps/editor/src/views/BaniyaDashboard.vue'
      provides: 'Uses Badge.vue for sensitivity/route badges'
      contains: 'import.*Badge'
    - path: 'apps/editor/src/components/dashboard/AuditTable.vue'
      provides: 'Uses Badge.vue for sensitivity/route badges'
      contains: 'import.*Badge'
    - path: 'apps/editor/src/views/WorkflowEditor.vue'
      provides: 'Uses EdgeLabel.vue in edge rendering'
      contains: 'import.*EdgeLabel'
    - path: 'apps/editor/src/components/canvas/BaniyaNode.vue'
      provides: 'Uses MiniExecutionBadge.vue for execution status'
      contains: 'import.*MiniExecutionBadge'
  key_links:
    - from: 'apps/editor/src/views/WorkflowList.vue'
      to: 'apps/editor/src/components/shared/Modal.vue'
      via: 'import + component usage'
      pattern: 'import.*Modal'
    - from: 'apps/editor/src/views/WorkflowList.vue'
      to: 'apps/editor/src/components/shared/EmptyState.vue'
      via: 'import + component usage'
      pattern: 'import.*EmptyState'
    - from: 'apps/editor/src/components/dashboard/AuditTable.vue'
      to: 'apps/editor/src/components/shared/Badge.vue'
      via: 'import + component usage'
      pattern: 'import.*Badge'
    - from: 'apps/editor/src/views/WorkflowEditor.vue'
      to: 'apps/editor/src/components/canvas/EdgeLabel.vue'
      via: 'import + edge template'
      pattern: 'import.*EdgeLabel'
    - from: 'apps/editor/src/components/canvas/BaniyaNode.vue'
      to: 'apps/editor/src/components/canvas/MiniExecutionBadge.vue'
      via: 'import + component usage'
      pattern: 'import.*MiniExecutionBadge'
---

<objective>
Wire 6 orphaned Vue components into the views that need them, replacing inline markup with shared component usage.

Purpose: Eliminate dead code, ensure component reuse, complete integration work from plans 05-04 and 05-05.
Output: All 6 components imported and used in appropriate views; inline markup replaced.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor/05-VERIFICATION.md
@.planning/phases/05-editor/05-04-SUMMARY.md
@.planning/phases/05-editor/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire shared components (Badge, Modal, Spinner, EmptyState)</name>
  <files>
    apps/editor/src/views/WorkflowList.vue
    apps/editor/src/views/BaniyaDashboard.vue
    apps/editor/src/components/dashboard/AuditTable.vue
  </files>
  <action>
    Replace inline markup with shared component imports in three files:

    **WorkflowList.vue:**
    - Import Modal.vue, EmptyState.vue, Spinner.vue from '../components/shared/'
    - Replace inline modal markup (lines ~39-57) with `<Modal>` component
    - Replace inline empty state markup (lines ~14-17) with `<EmptyState>` component
    - Replace inline `<div class="spinner">` with `<Spinner>` component
    - Remove the inline markup that these components replace

    **BaniyaDashboard.vue:**
    - Import Badge.vue from '../components/shared/'
    - Replace inline `class="badge badge-*"` spans with `<Badge>` component usage for sensitivity levels and route types
    - Map sensitivity levels to Badge props: critical→red, private→amber, internal→blue, public→green
    - Map route types to Badge props: local→green, hybrid→amber, cloud→blue

    **AuditTable.vue:**
    - Import Badge.vue from '../shared/'
    - Replace inline badge spans with `<Badge>` component for sensitivity and route columns
    - Use same color mapping as BaniyaDashboard

    Reference existing component interfaces:
    - Badge.vue: props `text`, `color`, `variant` (solid/outline)
    - Modal.vue: teleport to body, overlay click closes, ESC closes, slot content
    - Spinner.vue: configurable size
    - EmptyState.vue: illustration slot, title, description, action button

  </action>
  <verify>
    - grep for 'import.*Badge' in BaniyaDashboard.vue and AuditTable.vue
    - grep for 'import.*Modal\|import.*EmptyState\|import.*Spinner' in WorkflowList.vue
    - No inline `<div class="spinner">` remains in WorkflowList.vue
    - No inline modal markup remains in WorkflowList.vue
    - No inline empty state markup remains in WorkflowList.vue
    - vue-tsc --noEmit passes
  </verify>
  <done>
    All 4 shared components (Badge, Modal, Spinner, EmptyState) are imported and used in the views that need them. Inline markup replaced. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire canvas components (EdgeLabel, MiniExecutionBadge)</name>
  <files>
    apps/editor/src/views/WorkflowEditor.vue
    apps/editor/src/components/canvas/BaniyaNode.vue
  </files>
  <action>
    Integrate the two orphaned canvas components:

    **WorkflowEditor.vue — EdgeLabel:**
    - Import EdgeLabel from '../components/canvas/EdgeLabel.vue'
    - Add EdgeLabel to Vue Flow edge template/rendering
    - Pass edge handle name as prop to EdgeLabel
    - If Vue Flow uses custom edge template, wrap EdgeLabel in the edge template
    - If using default edges, add EdgeLabel as an overlay on edges

    **BaniyaNode.vue — MiniExecutionBadge:**
    - Import MiniExecutionBadge from './MiniExecutionBadge.vue'
    - Add MiniExecutionBadge component to the node template (bottom-right corner)
    - Wire it to show execution status: "$0.000" or "free" after execution, status icon
    - The component should receive execution data (cost, status) as props
    - If execution data is available via the workflows store or WebSocket, pass it through

    Reference existing code:
    - WorkflowEditor.vue already has Vue Flow integration with custom node types
    - BaniyaNode.vue already has a cost badge area (bottom-right) — integrate MiniExecutionBadge there
    - EdgeLabel.vue expects handle name prop
    - MiniExecutionBadge.vue expects execution result data

  </action>
  <verify>
    - grep for 'import.*EdgeLabel' in WorkflowEditor.vue
    - grep for 'import.*MiniExecutionBadge' in BaniyaNode.vue
    - EdgeLabel appears in WorkflowEditor.vue template
    - MiniExecutionBadge appears in BaniyaNode.vue template
    - vue-tsc --noEmit passes
    - vite build passes
  </verify>
  <done>
    EdgeLabel.vue is integrated into Vue Flow edge rendering in WorkflowEditor.vue. MiniExecutionBadge.vue is integrated into BaniyaNode.vue showing execution cost/status. Build passes cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification and cleanup</name>
  <files>
    apps/editor/src/views/WorkflowList.vue
    apps/editor/src/views/BaniyaDashboard.vue
    apps/editor/src/components/dashboard/AuditTable.vue
    apps/editor/src/views/WorkflowEditor.vue
    apps/editor/src/components/canvas/BaniyaNode.vue
  </files>
  <action>
    Run full build verification and fix any issues:
    - `pnpm --filter editor exec vue-tsc --noEmit` — TypeScript check
    - `pnpm --filter editor build` — Vite build
    - Fix any TypeScript errors from the component wiring
    - Fix any runtime errors from prop mismatches
    - Verify no orphaned components remain (all 6 are now imported somewhere)
    - Run `grep -r "import.*Badge\|import.*Modal\|import.*Spinner\|import.*EmptyState\|import.*EdgeLabel\|import.*MiniExecutionBadge" apps/editor/src/` to confirm all 6 components have at least one import
  </action>
  <verify>
    - vue-tsc --noEmit passes with zero errors
    - vite build completes successfully
    - All 6 previously orphaned components now have at least one import in the codebase
  </verify>
  <done>
    Build passes cleanly. All 6 orphaned components are now wired into the application. No dead code remains from this gap.
  </done>
</task>

</tasks>

<verification>
- All 6 components (Badge, Modal, Spinner, EmptyState, EdgeLabel, MiniExecutionBadge) have at least one import in the codebase
- No inline modal, spinner, empty state, or badge markup remains where shared components should be used
- EdgeLabel renders on canvas edges
- MiniExecutionBadge renders on executed nodes
- vue-tsc and vite build both pass
</verification>

<success_criteria>

- 10/16 → 16/16 must-haves verified (all orphaned components wired)
- VERIFICATION.md score improves from gaps_found to passed
- Zero orphaned components in the codebase
  </success_criteria>

<output>
After completion, create `.planning/phases/05-editor/05-06-SUMMARY.md`
</output>
